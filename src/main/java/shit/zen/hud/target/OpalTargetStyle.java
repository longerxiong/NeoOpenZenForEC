package shit.zen.hud.target;

/** Opal target HUD uses the same compact panel renderer as the legacy Round style. */
public final class OpalTargetStyle extends RoundTargetStyle {
    @Override
    public String getName() {
        return "Opal";
    }
}
